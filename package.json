{
  "name": "contract-companion",
  "private": true,
  "scripts": {
    "install:backend": "npm install express cors multer dotenv --prefix backend && npm install --save-dev nodemon --prefix backend",
    "install:frontend": "npm install axios --prefix frontend",
    "install:all": "npm run install:backend && npm run install:frontend",
    "start:all": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
